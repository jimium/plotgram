# 13 · 自研布局引擎的算法选型与实现路线图

> 本篇是前 12 篇的收敛：**如果现在从零开始做 tautcore 的布局层，具体该做什么、按什么顺序、每步怎么验收。**
> 所有选型都给出"为什么不选另一个"，避免后续反复。

---

## 1. 一页选型表（决策已定，不再讨论）

| 子问题 | 选型 | 为什么不选替代方案 |
|---|---|---|
| 主图种骨架 | **Sugiyama 分层**（01 篇） | TSM 正交（02）丢失流向语义、实现重；organic 无法表达层级 |
| 去环 | **Greedy-FAS**（Eades–Lin–Smyth） | DFS 反向质量差；精确 MFAS 是 NP 难 |
| 分层 | **Network Simplex**（最小加权跨度） | longest-path 太扁、dummy 多；Coffman–Graham 只在限宽场景优 |
| 层内定序 | **median sweep + transpose + best snapshot + 边权 1/2/8** | 纯 barycenter 质量差一档；ILP 不实用；sifting 可作后续增强 |
| 交叉计数 | **Barth–Jünger–Mutzel 累加树** $O(E\log V)$ | 朴素 $O(E^2)$ 在宽层上是热点 |
| 次轴坐标 | **Brandes–Köpf**（含 2020 勘误）起步，约束变多后换 **VPSC** | dot 的 network-simplex-x 慢；priority 法质量差 |
| 通用约束求解 | **VPSC**（Dwyer–Marriott–Stuckey） | 通用 QP 求解器过重；手写贪心必然长出特判 |
| 边路由（分层图） | **通道图 + A\*(带方向状态) + 区间着色 track + VPSC nudging** | OVG 在规则布局上是浪费；网格法有假阻塞 |
| 边路由（自由/拖动后） | **OVG（扫描线构造）+ 同上下游** | 通道图不适用于任意位置 |
| 树图种 | **Buchheim–Jünger–Leipert** | 朴素 RT 是 $O(n^2)$ |
| organic 图种 | **PivotMDS 初值 → SGD stress → PRISM 消重叠 → 装箱** | FR 需退火调参、不确定；SMACOF 慢于 SGD |
| 标签 | **空间预留（label dummy / 间距计入）+ 末尾 greedy labeling** | 纯事后标注必然重叠 |
| 端口 | **五档模型**（FREE/FIXED_SIDE/FIXED_ORDER/FIXED_RATIO/FIXED_POS），默认 FREE+按对侧序分配 | 路由阶段发明端口 = 不可修复的绕行 |
| 分组/泳道 | **边界 dummy + 受约束排序（PAV 投影）+ 组框作为 VPSC 变量** | 递归布局丢跨组边；事后调包围盒会重叠 |
| 四方向 | **Orientation Stage 坐标变换**，核心只实现 TB | 四套代码不可维护 |
| 引擎结构 | **Stage 装饰器链 + 强类型 LayoutData + Writer 类型化写权** | 平铺管线容易漏还原；字符串 KEY 的 DataProvider 易错 |

---

## 2. 分阶段路线图

每个阶段给出：交付物、验收标准（可自动检查）、预期难点。

### M0 · 地基（无算法）
**交付**
- `LayoutGraph` 纯几何模型（IndexMap 存储，稳定 ID）；
- Writer 类型（`LayerWriter` / `OrderWriter` / `CoordWriter` / `RouteWriter`），每个自由度一个；
- Stage 装饰器 trait + 流水线构建器；
- 诊断输出结构（分相耗时、指标、门控命中、约束报告）；
- **VPSC 求解器**（约 300–500 行，含 block merge/split、不可行检测）；
- 度量模块：交叉数（Bentley–Ottmann）、重叠检测（扫描线）、弯折统计。

**验收**
- VPSC 单元测试：随机约束集，断言 (a) 所有约束满足，(b) 目标值 ≤ 任意可行解（对小实例枚举验证）；
- 度量模块对手工构造图给出已知正确值；
- 同一输入连跑两次，`LayoutGraph` JSON 完全一致。

**难点**：Writer 改造要一次做对，后期补很痛。VPSC 的 split（负乘子）容易漏，漏了会在"约束冗余"场景给出次优解。

---

### M1 · 分层主路径（能出图）
**交付**：P1 Greedy-FAS → P2 Network Simplex → 长边 dummy 化 → P3 median+transpose(+边权 1/2/8, best snapshot) → P4 Brandes–Köpf → P5 dummy 链还原为折线。Orientation Stage、自环 stage、平行边 stage、分量 stage + 行装箱、NormalizeCoordinates。

**验收**
- 硬断言：节点无重叠、边不穿节点、确定性双跑一致；
- 软指标基线：在 AT&T DAG 集（或自建 DAG 集）上记录 crossings / bends / edge_len / area / dummy 数；
- 长边直线率 > 90%（BK 应该做到）；
- 微型手工集的坐标 insta 快照。

**难点**
1. Network Simplex 的 cut value 增量维护（lim/low 区间）容易写错 → 用"全量重算 cut value"版本先跑通，再优化；
2. Brandes–Köpf 的 type-1 conflict 标记与 compaction，**务必对照 2020 勘误**；
3. dummy 数爆炸：先加"长边链"抽象，别让 P3 每层独立处理。

---

### M2 · 正交路由（决定观感）
**交付**：通道图构造（层间/层内空隙）→ A*(状态含方向) → 区间着色 track 分配 → VPSC nudging（端口对齐 + 最小段长 + 标签避让）→ 圆角/箭头装饰。走廊 demand 回写层间距。

**验收**
- 硬断言：无边穿节点、无两条边完全重合、圆角段长足够；
- 指标：总弯折数、`<30°` 交叉占比（正交图应≈0）、总曼哈顿长度；
- 门控：边数超阈值时降级并在诊断中报告。

**难点**
1. A* 状态忘记含方向 → 弯折惩罚失效（最常见 bug）；
2. 走廊 demand 回写必须是"参数回写"而非"路由阶段挪节点"，否则打破写权；
3. 反向对边重合：根因在端口分配而非路由（见 03 篇 §7 表）。

---

### M3 · 端口与标签（专业度分水岭）
**交付**：端口五档模型落 IR + DSL；FREE 端口的"按对侧 x 排序 + 均匀分布"；端口数回写节点最小尺寸；FIXED_ORDER 的端口 dummy 参与定序；label dummy（边标签）+ 节点标签计入有效包围盒 + 末尾 greedy labeling。文本测量（真实字体度量 + CJK 断行）。

**验收**
- 端口序与邻居序一致性断言（不产生"凭空交叉"）；
- 标签重叠面积 = 0；标签压边长度指标；
- 中英混排标签的尺寸跨平台一致（WASM 与原生一致）。

**难点**：文本测量的工作量常被低估（字体加载、kerning、CJK 断行、WASM 字体分发）。建议尽早做，因为它是节点尺寸的输入，影响一切。

---

### M4 · 分组与泳道（结构表达力）
**交付**：包含树模型；组边界 dummy + 受约束排序（PAV 投影）；组框作为 VPSC 变量（含 padding、标题高度、组间距）；跨组边的边界穿越点；PartitionGrid（行=层区间、列=层内分段）；组折叠/展开 + 布局缓存。

**验收**
- 约束满足断言：组内节点确在组框内、组框互不重叠、泳道内节点确在列区间内、层跨泳道全局一致；
- 嵌套 3 层以上的 case 无重叠；
- 折叠→展开→折叠的布局稳定（位移 ≈ 0）。

**难点**：组与泳道要复用同一套"层内连续块"机制，否则两套逻辑会互相冲突。约束冲突的优先级与降级必须显式（08 篇 §4）。

---

### M5 · 树 / organic / 其它图种
**交付**：Buchheim 树 + NodePlacer 策略集（centered/bus/double-line/dendrogram）；TreeReductionStage；Radial（角需求自底向上传播）；Balloon；PivotMDS + SGD stress + PRISM + 装箱；circular（谱序 + BCC 树）。

**验收**：各图种硬断言 + 各自的专属指标（树：宽度/对称；organic：stress 值单调下降、消重叠后重叠=0）。

---

### M6 · 交互与稳定性
**交付**：from-sketch（由当前坐标反推层与序）；PartialLayout（fixed/partial）；增量路由（依赖索引 + hysteresis）；FixNode 锚定；稳定性指标（位移、正交序违反率、k-NN 保持）。

**验收**：拖一个节点后，未受影响区域位移 = 0；正交序违反率低于阈值；重路由只涉及受影响边（可断言重路由边数）。

---

## 3. 贯穿全程的三条纪律（对应 AGENTS.md）

1. **每个自由度一个写者**：用 Writer 类型强制。新增机制前先回答"这个自由度的写者是谁"。
2. **下游只展开、不推翻**：唯一允许的反向影响是**参数回写**（走廊 demand → 层间距；端口数 → 节点尺寸；标签尺寸 → 间距），且必须发生在上游相执行**之前**。
3. **不按图种分支**：图种差异全部走 profile → 参数。看到 `if diagram_type ==` 就是设计出问题了。

**自检清单**（每次修 bug 前问自己）：
- 这个症状是"上游决策错"还是"下游落笔错"？
- 我的修法是"加特判"还是"把自由度上提"？
- 这个修法在别的图种上会不会反噬（基准分图种报告能否证明）？

---

## 4. 反模式清单（历史上反复出现的坑）

| 反模式 | 后果 | 正解 |
|---|---|---|
| 路由阶段发现重叠就挪节点 | 交叉数/序失效，改一处坏两处 | 走廊 demand 回写层间距 |
| 事后调整组框包围盒 | 组框互相重叠或把边挤出去 | 组框边界作为 VPSC 变量 |
| 用 HashMap 迭代驱动算法 | 同输入不同输出，不可调试 | IndexMap/BTreeMap + 稳定 tie-break |
| 标签事后避让 | 必然重叠或飘远 | 空间预留（label dummy） |
| 四方向写四套代码 | 特性只在一个方向可用 | Orientation Stage |
| 路由阶段临时发明端口 | 无法修复的绕行与交叉 | 端口分配上提为独立相 |
| 只看总指标平均值 | 一个图种改善掩盖另一个退化 | 分图种报告 + top-N 退化榜 |
| 用 `--release` 之外的构建测性能 / 信旧 binary | 结论错误 | 按 AGENTS.md 的验真纪律 |
| 在会进 WASM 的代码里用 `Instant` | WASM panic / 非确定 | 项目 perf 抽象 |
| 一次改多个相然后跑基准 | 无法归因 | 一次只改一个机制 |

---

## 5. 投入产出比排序（如果资源有限）

按"每单位工作量的观感/能力提升"排序：

1. **P3 的边权 1/2/8 + best snapshot**（几十行）——立刻让长边变直、图变整齐。
2. **Orientation Stage**（一百多行）——一次拿到四个方向。
3. **VPSC**（几百行）——服务五处需求（间距、消重叠、nudging、紧致化、组框）。
4. **端口按对侧序排序分配**（几十行）——消除大量"凭空交叉"。
5. **label dummy 预留标签空间**（几十行）——彻底解决标签重叠。
6. **走廊 demand 回写层间距**（上百行）——解决"边挤在一起/穿节点"。
7. **层次边捆绑**（约五十行）——大边数图的可读性质变。
8. **substructure detection（星/链/环）**（数百行）——organic 图种观感质变。
9. **from-sketch**（上百行）——交互场景的用户价值极高。
10. **分图种基准报告**（脚本层）——让上述每一项的收益可证明。

**建议顺序**：先做 10（度量），再按 1–6 依次做，每做一项跑一次基准并记录。这样每一步都有证据，也不会互相掩盖。

---

## 6. 延伸篇目（14–20）与本路线图的对应

本篇成稿后补充的 7 篇，把上面每个阶段的"怎么做"落到更细：

| 阶段 | 配套篇目 | 直接用得上的内容 |
|---|---|---|
| M0 地基 | [14 · 零件库](14-图论与优化工具箱.md) | Network Simplex / VPSC / 扫描线 / 区间着色 / Fenwick 的实现要点与"自研 or 不做"决策表 |
| M0 地基 | [20 · 参数映射](20-图种profile与参数映射.md) | 参数域设计三准则、四层覆盖、**DemandBoard 回写机制**、ADR-001 的 CI 守卫 |
| M1/M2 | [16 · 性能与 WASM](16-大图性能与wasm.md) | 复杂度预算表（**优化只有 P3 与路由两个真目标**）、L0–L7 降级阶梯、WASM 红线 |
| M2 路由 | [18 · 超图与 EDA](18-超图总线与eda路由.md) | **层间路由 = 通道布线**、全局/详细两段式、history cost + 拆线重布、总线主干 = 加权中位数 |
| M2/M3 落笔 | [15 · 落笔层](15-几何与落笔层.md) | 形状裁剪、规范化六步流水线、圆角降半径、箭头族、退化清单、量化与确定性 |
| M5 图种 | [19 · 序列图与一维排列](19-序列图与一维排列.md) | 一维排列器（MinLA，服务生命线/泳道/组内序）、主轴分层、区间嵌套器 |
| M6 交互 | [17 · 动画与视图](17-动画过渡与视图.md) | 分阶段过渡、边形变的三种策略、hysteresis 折扣、zoom-to-cursor、命中测试 |

### 对 §5 投入产出比排序的两处补充

读完 14–20 后，有两项应插进前列：

- **插在第 3 位之后：一维排列器（约 200 行）** —— 一份实现服务生命线排序、泳道排序、组内排序、circular 排序四处（19 篇 §7.1）。
- **插在第 6 位（走廊 demand 回写）之后：history cost + 拆线重布（约 150 行）** ——
  它是"某处走廊挤爆"的**正确**解法，取代反模式清单里的"给某条边加特判"（18 篇 §5.3）。

### 一个建议尽早做的一次性投入

[20 篇 §6 检验 1](20-图种profile与参数映射.md) 的"新图种落地测试"：拿 BPMN 与甘特图，只用参数表达它们，把填不出来的格列成参数域 TODO。
成本约半天，收益是**提前知道参数域的空洞在哪**，避免 M3/M4 做到一半才发现要重构参数结构。
