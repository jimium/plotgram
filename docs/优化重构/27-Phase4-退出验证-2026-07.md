# Phase 4 退出验证（Group IR / builder / oneshot，2026-07-25）

## 裁决（必读）

| 维度 | 结论 |
|---|---|
| **切片交付（4.A–4.E + 4.V）** | ✅ 可主张完成：代码、基线、红线（正交族穿组=0 / `det=true`）到位 |
| **架构退出判据**（doc 23：写权=1、builder=1、Group 进求解器） | ❌ **未达成**——见下方「半完成项」与「记债 + 何时补执行」 |
| **下一步** | 可开 Phase 5 主切片；**进 Phase 5 第一周前**须把写权仪表与 Group IR 降级声明对应的补执行做完（或显式砍进 Phase 5 范围） |

> 对外说法建议：「Phase 4 **功能里程碑**退出，进入 Phase 5 准备」；**不要**说「Group 单次求解 / 写权收敛已完成」。

## 范围

按计划完成 **4.A–4.E** 的切片动作；**未做** Phase 5（PRS 删除、`RouteDemand` 前馈、rank 轴进求解器、`algo == "architecture"` 清零）。

## 切片状态（降级后口径）

| 切片 | 状态 | 实际达成 | 未达成 / 降级说明 |
|---|---|---|---|
| 4.A | ✅ | 删 `assign_coordinates_intra` / neighbor-pull；`write_counter` + PRS 窗口；route 前 WARN | 计数覆盖不全；threshold=8 仍 WARN，非 =1 panic |
| 4.B | ⚠️ **IR 占位** | `GroupVariable` + `GroupContainment` / `GroupSiblingSeparation` 类型；`shadow_audit` / `compile_group_variables` | 边界 `VarId=None`；硬槽位无生产构造；投影显式跳过；**非**进求解器 |
| 4.C | ⚠️ **helper 合一** | `builder_common`；flat / arch / intra 共用 | 构造入口仍 ≥3（+ mindmap 第 4）；非单一 `CoordinateProblemBuilder` |
| 4.D | ✅ 主链 | phase_d 去掉 demand-gap / infra-rebalance / same-rank-y | flat `coordinate.rs` anchor 安全网仍在；`space_budget::enforce_*` 函数未清（仅不走 phase_d） |
| 4.E | ⚠️ **过程链 oneshot** | phase_d：sibling→expand→(Fit)shrink；`refresh_before_route` 跳过二次完整 frame；PRS 暂留 | **非** group-aware `CoordinateKernel::solve`；runner 上 snap→refresh→recompute→PRS→expand 仍多段写 |
| 4.V | ✅ | 基线 + 红线 + 文档 | product 质量相对 3.x 有有界退化（§8 接受） |

## 验证

```bash
cargo check -p plotgram-core
cargo test -p plotgram-core --lib          # 941 passed
cargo run -p plotgram-cli -- render showcase/architecture/product.cloud-native.pgm -o /tmp/out.svg
# 双渲 sha 一致 → det 抽样 OK
./benchmarks/snapshot.sh --tag phase4-group-solver
./benchmarks/compare.sh benchmarks/baselines/2026-07-25-211934-phase3-joint-solve.json \
                        benchmarks/baselines/2026-07-25-212621-phase4-group-solver.json
```

对照基线：`phase3-joint-solve`（`phase4-start` 软链同份）。  
正式抬基线：`2026-07-25-212621-phase4-group-solver`。

### compare 摘要（相对 Phase 3.x）

- **正确性轨 PASS**（穿组 / det）
- 质量/美学：少数 product（cloud-native / microservices 等）`node_fp` / `exact_sev` / bends·crossings 有界变化（§8 门禁暂停）

## 红线

- 正交族穿组 = 0（正确性硬）
- `det=true`
- §8 门禁质量轨仍暂停阻挡

---

## 记债 + 何时补执行

> **原则**：债必须带「触发时机」。到点未做则阻塞对应 Phase 的退出，不可再静默滚到下一轮。

| ID | 债 | 现状证据 | **何时补执行** | 完成判据 |
|---|---|---|---|---|
| **D4-1** | group 写权仪表做实 | ✅ 2026-07-25：生产改写点均 `record_group_write_at`；threshold=1；cloud-native main≈11（诚实暴露多写，闭合→D4-2） | **已完成（Week 0）** | 正式 route 前摘要可观测；目标=1 仍靠 D4-2 |
| **D4-2** | Group IR：shadow → 生产求解 | `VarId=None`；无 `GroupContainment` 生产 push；`let _ = shadow_audit...` | **Phase 5 与 rank 轴同批，或 Phase 5 第一周并行**（布局侧核心债） | 边界变量分配 + 成员⊆容器 / 同级分离进投影；生产写权来自一次 `solve`，过程链 `expand/shrink/sibling` 可删或变断言 |
| **D4-3** | builder 入口真正 =1 | flat/arch/intra 三入口 + mindmap | **Phase 5 中段（与 LayoutContract 一并）**；mindmap 最迟 Phase 5 末 | 主路径单一 `CoordinateProblemBuilder`；mindmap 迁入或永久第二入口写死在 CI 白名单 |
| **D4-4** | RoutingContract 单一真源 | `PreparedRoutingInput::prepare` 仅 `compile`，无 periphery/trunk；ShareTrunk **注入**仍经 `profile.semantic_merge` | **Phase 5 删 PRS 之前**（否则 PRS/增量路径契约继续分叉） | prepare 合并 draft 级 intents；`with_share_trunk` 由 recipe 显式注入，不经图种 profile |
| **D4-5** | Phase 3 卫生：`port_solver` / `&mut [EdgeLayout]≤3` / 空壳 stub | 26 文档已记 | **Phase 5 末或 Phase 6 typestate**（与写权冻结同批） | LOC 下降 + 签名数达标 |
| **D4-6** | PRS / `spacing_demand_probe` / `algo==` | runner 仍保留 | **Phase 5 主切片（doc 23）** | 路由前无预览路由；`algo==` =0；RouteDemand 前馈 |
| **D4-7** | product 质量回看 | cloud-native sev↑、microservices bends/crossings↑ | **Phase 5 退出时对照 `phase4-group-solver`**；§8 恢复前须持平或显式抬基线 | product-gate 严重度 ≤ Phase 4 基线（恢复门禁后） |

### 建议执行顺序（开 Phase 5 时照抄）

```text
Week 0（进 Phase 5 前门禁，~1 天）
  └─ D4-1 写权仪表做实 + 更新本文件勾选

Week 1（Phase 5 启动并行）
  ├─ D4-4 契约单一真源（为删 PRS 铺路）
  └─ D4-2 Group IR 接线（可与 rank 轴并行设计）

Week 2–3（Phase 5 主切片）
  └─ D4-6 删 PRS / probe / algo==；RouteDemand 前馈

Week 3–4
  ├─ D4-3 builder 合一收口（含 mindmap）
  └─ D4-5 port_solver / EdgeLayout 签名

退出前
  └─ D4-7 compare vs phase4-group-solver + 抬基线 note
```

勾选栏（补执行完成后改 ✅）：

- [x] D4-1 写权仪表（2026-07-25：覆盖 merge_egb / equalize / sibling / apply_frame / realign / compose / center_rows / shell_expand；threshold=1；cloud-native 实测 main≈11，站点链见 perf `[group_write]`）
- [x] D4-2 Group IR 进求解器（Phase 5：VarId + 投影；Equal/SharedLines 过程链暂留）
- [x] D4-3 builder 入口门面（`coordinate::builder`；mindmap 白名单第二入口）
- [x] D4-4 PreparedRouting 契约富化
- [ ] D4-5 port_solver / EdgeLayout（**结转 Phase 6**，见 [29](29-Phase5-退出验证-2026-07.md)）
- [x] D4-6 PRS / probe / algo==
- [x] D4-7 质量回看（Phase 5 snapshot `phase5-rank-route-demand`）

---

## Phase 5 承接

债的**执行时机与勾选**以 [28-Phase5-启动说明与执行顺序-2026-07.md](28-Phase5-启动说明与执行顺序-2026-07.md) 为准；  
总方案摘要见 [23](23-布局与路由第一性原理重构方案-2026-07.md) §Phase 5。  
**开 Phase 5 前先做 D4-1**；删 PRS 前先做 D4-4。

---

## 与 doc 23 Phase 4 退出判据对照

| doc 23 判据 | 本轮实际 |
|---|---|
| group 写入 = 1（运行时断言） | ❌ 未；仅 WARN 仪表且覆盖不全 |
| `CoordinateProblem` 构造点 = 1 | ❌ helper 合一，入口仍多 |
| `recipes/architecture/` LOC ≤ 4k / 净删 ≥ 4.5k | 未作为本轮硬门禁度量 |
| product-gate 严重度不高于 Phase 3 | ⚠️ 正确性达标；质量轨有有界退化（§8） |

---

## 基线速查

| Tag | 路径 |
|---|---|
| Phase 3.x | `benchmarks/baselines/2026-07-25-211934-phase3-joint-solve.{json,md}` |
| Phase 4 start（= 3.x） | `benchmarks/baselines/2026-07-25-phase4-start.json` |
| Phase 4 抬基线 | `benchmarks/baselines/2026-07-25-212621-phase4-group-solver.{json,md}` |
| latest | `benchmarks/baselines/latest.{json,md}` |
