# Group 一等公民 — 退出验证（G4→G5→G6，2026-07-25）

> 方案：[31-Group一等公民与终态收敛方案](31-Group一等公民与终态收敛方案-2026-07.md)  
> 对照基线：`gfc-g3-close` / `gfc-pre-plain-container`；退出 tag：`group-first-class`

## 裁决

| 阶段 | 结论 |
|---|---|
| **G4 RouteDemand 闭环** | ✅ 走廊/跨组边负载 → H-G3；orthosketch 只抬 `side_gutters`；删除 runner `post_route_shell_expand`；溢出 → Degraded；`FrozenNodeProduct` 校验 groups 指纹 |
| **G5 builder = 1** | ✅ 生产经 `CoordinateProblem::build`；内联 `{` 生产 =0；`check-builder-entry.sh` + CI；mindmap 亦迁入门面 |
| **G6 LexCost + 收口** | ✅ `DefaultScorer` 词典序 `(hard_residual, bends, length)`；罚项项数棘轮 ≤13（现 12）；snapshot + 手册 A3 |
| **红线** | ✅ 正交族穿组=0 / 全样本 `det=true`（saas-schema ER 穿组为 G3 既有债） |

## 写权

| 指标 | G3 | G4–G6 退出 |
|---|---|---|
| cloud-native `main=` | 4 | **2** |
| sites | compute → feedforward → post_route → canvas | **compute_group_bounds → canvas_finalize_translate_groups** |
| 目标 | 1 | 仍记债 →1（canvas 平移令牌） |

## 验证

```bash
cargo test -p plotgram-core --lib          # 916 passed
./benchmarks/scripts/check-penalty-ratio.sh
./benchmarks/scripts/check-semantic-isolation.sh
./benchmarks/scripts/check-group-writes.sh   # main≤2
./benchmarks/scripts/check-builder-entry.sh  # 生产 inline=0
cargo run -p plotgram-cli -- render showcase/architecture/product.cloud-native.pgm -o /tmp/gfc-final.svg
./benchmarks/snapshot.sh --tag group-first-class --runs 1
```

## 完成度摘要

### G4
1. `pair_gaps_from_cross_group_edge_loads` / `pair_gaps_from_corridor_demands` → `draft_boost_h_g3`（arch_builder + phase_d gutters）
2. `feedforward_shell_from_orthosketch` 不再写 groups
3. runner 删除 post_route 扩壳；`route_shell_overflow_remaining` → degraded
4. `FrozenNodeProduct::assert_unchanged` 含 groups 指纹

### G5
1. `CoordinateProblem::build` 统一门面；flat / arch / intra / mindmap / main_axis / refine 生产路径均经此
2. 白名单收缩为 kernel 测试辅助；`check-builder-entry.sh` 接 CI

### G6
1. `CandidateScorer::lex_cost` / `prefer`；DefaultScorer 实现词典序
2. `MAX_SOFT_ITEMS=13`；`check-penalty-ratio.sh` 断言项数
3. 手册 §1.0 A3：「group 是变量；无几何 hint」

## Snapshot

- `benchmarks/baselines/2026-07-25-233904-group-first-class.json`
- `benchmarks/baselines/2026-07-25-233904-group-first-class.md`
- 已 sync `latest.*`

相对 `gfc-g3-close`：cloud-native `exact_sev`/`tight_sev` 持平；正交族 architecture product 样例 `edge_crosses_group_interior=0` / `pierce_group_sev=0`；全样本 `det=true`。  
既有债：`product.saas-schema.pgm`（ER）穿组 1 / sev≈558，与 G3 基线数字相同，非本轮引入。  
note 含 `raise product:` 与 G-pre…G6 摘要。

## 已知债（不挡退出）

| 项 | 说明 |
|---|---|
| 写权 →1 | canvas translate 仍计 1；理想合并进 materialize 令牌 |
| shell overflow → degraded | 无 post_route 扩壳；cloud-native 可见 `route shell overflow remaining` |
| FanIn 邻近 | `s32b` 容差 50→72（G4 几何略散） |
| 罚项项数 →≤8 | 现 12 / 上限 13；未再砍项 |
| mindmap LayoutContract | 已用 `::build`，树形 objectives 组装仍在 recipe |
| swimlane/table DSL | 见 doc 31 §9；不复活几何 hint |
