# Phase 6 退出验证（固化 / first-principles-final，2026-07-25）

## 裁决

| 维度 | 结论 |
|---|---|
| **6.1 罚项注册表 + 量级 CI** | ✅ `objectives.rs`；`check-penalty-ratio.sh` max/min ≤100；已接 CI |
| **6.2 语义隔离 + D4-5 + mindmap** | ✅ `check-semantic-isolation.sh`；`pub fn &mut [EdgeLayout]`=1；mindmap 白名单；空壳签名删除；`PortAssignment` 只读 |
| **6.3 写权 typestate** | ✅ `FrozenNodeProduct` 私有字段；`GroupShellMut`；ports newtype 只读 getter |
| **6.4 写权阈值 + Degraded 可见** | ✅ group 写权阈值 ≤3（目标=1 记债）；recipe 透传 `orthogonal_debug`；`[solver-status]`；bench `ortho.degraded_count` |
| **6.5 退出** | ✅ `AGENTS.md` §8 已失效；手册 §1.0 六条公理；snapshot `first-principles-final` |
| **红线** | ✅ 穿组=0 / `det=true` |

## 验证

```bash
cargo check -p plotgram-core
cargo test -p plotgram-core --lib          # 941 passed
./benchmarks/scripts/check-penalty-ratio.sh
./benchmarks/scripts/check-semantic-isolation.sh
cargo run -p plotgram-cli -- render showcase/architecture/product.cloud-native.pgm -o /tmp/out-phase6.svg
./benchmarks/compare.sh benchmarks/baselines/2026-07-25-220123-phase5-rank-route-demand.json \
  benchmarks/baselines/2026-07-25-222401-first-principles-final.json
./benchmarks/snapshot.sh --tag first-principles-final
```

对照基线：`2026-07-25-220123-phase5-rank-route-demand`。  
正式棘轮基线：`2026-07-25-222401-first-principles-final`。

### compare 摘要（相对 Phase 5）

- **正确性轨 PASS**（穿组 / det）
- **美学轨 product PASS**（crossings/bends）；cloud-native `hugging_violations` 3→4 记观测
- **质量轨**：Phase 5 样本 `ortho: null`；Phase 6 接通 A6 后 `degraded_count` 首次可见。compare 对「基线无 ortho」记 WARN 而非硬 FAIL；正式 note 带 `raise product:` 说明非质量退步
- **§8**：标注已失效；后续 product 质量轨恢复硬阻挡（以本品为新基线）

## 机制落地对照（doc 23 Phase 6）

| 机制 | 落点 |
|---|---|
| 罚项注册表 | `crates/plotgram-core/src/layout/routing/objectives.rs` |
| 量级 CI | `benchmarks/scripts/check-penalty-ratio.sh` |
| 语义隔离 CI | `benchmarks/scripts/check-semantic-isolation.sh` |
| typestate | `FrozenNodeProduct` / `GroupShellMut` / `PortAssignment` / materialize 链 |
| 写权棘轮 | `write_counter::warn_if_group_writes_excessive(3)` |
| Degraded 可见 | recipe→`RoutingProduct.orthogonal_debug`；runner route_audit 合并 |

## 显式记债（不挡退出）

| 项 | 说明 |
|---|---|
| Group 写权生产 =1 | 阈值已 ≤3；Equal/SharedLines 过程链仍多站点 |
| Cross+Main 合成单次 solve | 仍 2 次 |
| port_solver LOC 深砍 | 已只读化 + 权重可迁 objectives；未硬砍半 |
| A4 LexCost | 词典序 scorer 仍演进中 |
| degraded_count 语义 | 现为内核累加计数（可能含多轮）；后续可收束为「边数」并带 reason |

## 回写

- [23](23-布局与路由第一性原理重构方案-2026-07.md) Phase 6 勾选完成
- [29](29-Phase5-退出验证-2026-07.md) D4-5 / mindmap / §8 结转项闭合
- 手册 §1.0 公理表；`AGENTS.md` §8 已失效
