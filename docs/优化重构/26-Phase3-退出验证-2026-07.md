# Phase 3 退出验证（端口/lane/bundle 联合求解，2026-07-25）

## 复核结论（再审计）

**总体：可退出 Phase 3 主切片**——Legacy 双轨与生产后处理主路径已清；红线（正交族穿组 / 抽样 det / 测试）达标；质量相对 Phase 2 未恶化。

## Phase 3.x 收口（2026-07-25，进 Phase 4 前门禁）

| 项 | 状态 |
|---|---|
| `./benchmarks/snapshot.sh --tag phase3-joint-solve` | ✅ `benchmarks/baselines/2026-07-25-211934-phase3-joint-solve.{json,md}` |
| `RoutingContract::fill_prefer_periphery` + `with_share_trunk` 接线 draft | ✅ |
| `run.rs` merge 读 `contract.has_share_trunk()` | ✅ |
| `build.rs` `prefer_periphery` 优先读 contract | ✅ |
| LexA* `ResourceEdge.capacity` → `q1_hard_residual` | ✅（真 H4 第一步） |

**仍记债（不挡 Phase 4）**：掏空 `port_solver`、`&mut [EdgeLayout]≤3`、删空壳 stub/lane 导出。

---

## 完成项

| 切片 | 状态 |
|---|---|
| 3.1 删 Legacy / corridor / channel / `RouteKernelKind` | ✅ |
| 3.2 端口候选 → IR H4；rip-up 同环换端口 | ✅（生产=rip-up，非完整 RouteProblem 求解） |
| 3.3 lane 不 POST 平移；间距走 rip-up / H5 审计 | ✅ |
| 3.4 生产路径删 stub/dock/trunk 与 U 形穿组补丁 | ✅（空壳保留） |
| 3.5 `sanitize` 断言化 | ✅ **85 LOC** |
| 3.6 merge 实现不读 `DiagramType`；ShareTrunk 类型就位 | ✅（3.x 契约驱动） |
| 3.7 test / cli / 清单 / 红线 | ✅ |
| 3.x 正式基线 + 契约 + capacity 残差 | ✅ |

## 正确性红线

- product-gate **正交族** `edge_crosses_group_interior` = **0**
- `det=true` 抽样 OK
- 日常验证：`cargo check/test -p plotgram-core`（勿默认 `--release`）

## 硬约束债

见 [26-Phase3-硬约束违反清单-2026-07.md](26-Phase3-硬约束违反清单-2026-07.md)。

## Phase 4 入口

见计划 Phase 4.A–4.E；基线对照 tag：`phase3-joint-solve`（可与 `phase4-start` 共用若刚采完）。
