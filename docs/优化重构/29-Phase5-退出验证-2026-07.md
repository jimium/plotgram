# Phase 5 退出验证（rank / RouteDemand / Group IR，2026-07-25）

## 裁决

| 维度 | 结论 |
|---|---|
| **主切片（D4-4 / D4-6 / rank / builder 门面）** | ✅ 完成 |
| **D4-2 Group IR** | ✅ 边界 VarId + containment/sibling 进投影与 MinSeparation；phase_d 仍保留 Equal/SharedLines 过程链（IR `refine` 会拆左缘，暂不替代 sibling） |
| **D4-5 port_solver / EdgeLayout** | ✅ **Phase 6 闭合**：删空壳 `&mut [EdgeLayout]`；`pub fn` 签名 ≤3；`PortAssignment` 只读；mindmap CI 白名单 |
| **mindmap builder** | ✅ Phase 6：CI 白名单第二入口（`check-semantic-isolation.sh`） |
| **红线** | 穿组=0 / `det=true` 硬；~~§8 暂停~~ → Phase 6 已失效 §8 |

## 已完成动作

| 债 / 切片 | 落地 |
|---|---|
| **D4-4** | `PreparedRoutingInput::prepare` 富化 periphery / ShareTrunk；draft/recipe 消费 `input.contract` |
| **D4-6** | 删 runner LexA* preview PRS；`feedforward_shell_from_orthosketch`（曼哈顿估溢出）+ 正式 route 后 `post_route_shell_expand`；RouteDemand/反馈边抬缝前馈；`GroupFrameSpec.architecture_recipe` |
| **rank / Main** | `main_axis::solve_main_axis_layer_tops`；flat + intra 用 Main 轴二次求解；pre-route 不再 `enforce_vertical_rank_gaps` |
| **D4-2** | `group_ir::attach_group_ir_cross` + `project_group_constraints`；arch builder 可挂 IR |
| **D4-3** | `kernel/coordinate/builder` 统一再导出；flat/arch/intra 经 `builder_common`；mindmap 白名单第二入口 |

## 验证

```bash
cargo check -p plotgram-core
cargo test -p plotgram-core --lib          # 939 passed
cargo run -p plotgram-cli -- render showcase/architecture/product.cloud-native.pgm -o /tmp/out-phase5.svg
./benchmarks/compare.sh benchmarks/baselines/2026-07-25-212621-phase4-group-solver.json <current>
./benchmarks/snapshot.sh --tag phase5-rank-route-demand
```

对照基线：`2026-07-25-212621-phase4-group-solver`。  
正式抬基线：`2026-07-25-220123-phase5-rank-route-demand`。

### compare 摘要（相对 Phase 4）

- **正确性轨 PASS**（穿组 / det）
- 质量/美学：§8 下接受 node_fp / tight_sev / crossings 有界变化；正式抬基线 `2026-07-25-220123-phase5-rank-route-demand`

1. ~~**D4-5**~~ → 见 [30-Phase6](30-Phase6-退出验证-2026-07.md)  
2. ~~**mindmap**~~ → Phase 6 CI 白名单落地  
3. Group 写权 →1：Equal/SharedLines 与 IR 投影合一后删 phase_d sibling 过程链（仍记债）  
4. ~~写权 typestate / 罚项注册表 / 删 `AGENTS.md` §8~~ → Phase 6 完成

## 残余风险

- Cross+Main 各一次 solve（语义上仍两次）；向「一次 layout 求解会话」收敛留给后续  
- `enforce_vertical_rank_gaps` 函数仍在（测试 / 兼容），生产 pre-route 已不调用  
- §8 下 product 质量相对 Phase4 允许记债抬基线
