# 23 - Atlas 分阶段推进方案

> 日期：2026-07-26（Stage 7 收口 2026-07-27）  
> 状态：**已完成（Stage 0–7）**；门禁豁免 [`AGENTS.md`](../../AGENTS.md) §10 已失效  
> 设计依据：[`22-Atlas下一代布局与路由架构-总纲`](22-Atlas下一代布局与路由架构-总纲-2026-07.md)  
> 立场依据：[`21-Hierarchical统一内核与泳道语义`](21-Hierarchical统一内核与泳道语义-可行性与演进建议-2026-07.md)  
> 实证依据：[`25-相I可行率探针报告`](25-Atlas-相I可行率探针报告-2026-07.md) · [`27-channel审查与改造需求`](27-Atlas-channel模块审查与改造需求-2026-07.md)

---

## 0. 推进原则

### 0.1 三条铁律

1. **每个 Stage 结束时，主干必须能出图。** 允许观感退化，不允许「中间态跑不通」。
2. **每个 Stage 必须关掉它替换掉的旧路径。** 不允许「新旧长期并存」——双轨期限定在单个 Stage 内。
3. **对拍替代阻断。** 门禁关了（§10），但每个 Stage 有明确的**对拍口径**：新旧管线同图集跑，产出差异报告；退化要能说清是哪一类。

### 0.2 门禁关闭期的替代品

| 原门禁 | 本期替代 |
|--------|----------|
| `check-*.sh` 棘轮硬 FAIL | Stage 0 的**影子对拍报告**：新旧节点坐标 diff、lint diff、SVG 并排 |
| `compare.sh` product 硬 FAIL | 同一脚本 report-only 模式，人工判读 + 每 Stage 记「已知退化清单」 |
| CI `cargo test` 阻断 | `continue-on-error`；Stage 7 按新架构不变量重建 |

**仍然硬的**（§10 未豁免）：确定性、WASM 禁 `std::time`、禁图名特判、用 `cargo run -p plotgram-cli` 验真。

### 0.3 影子执行（Shadow）机制

贯穿 Stage 1–6 的核心工具：

```text
同一个 .pgm
   ├── LegacyPipeline  → LayoutResult_legacy
   └── AtlasPipeline   → LayoutResult_atlas
                ↓
        ShadowReport（差异报告）
          · 节点坐标 diff（逐节点 dx/dy，统计 p50/p95/max）
          · 组框 diff
          · lint 计数 diff（穿组 / 穿节点 / 交叉 / 折点）
          · 相 I 可行性（Plan 是否成功）与相 II 松弛级别
          · SVG 并排 HTML
```

切换开关：`PLOTGRAM_PIPELINE=atlas|legacy|shadow`（默认随 Stage 推进而变）。

---

## 1. Stage 总览

| Stage | 名称 | 核心交付 | 主干默认管线 | 预期退化窗口 |
|-------|------|----------|--------------|--------------|
| **S0** | 地基与对拍 | Provenance、Occupant/Demand 类型、Shadow 框架 | legacy | 无（纯新增） |
| **S1** | Plan IR 与表达力验证 | `Plan` 结构 + Legacy Adapter；channel L1–L8 合法化 | legacy | 无（旁路） |
| **S2a** | 度量相骨架（无组） | engines→kernel、Atlas 接管无组 flowchart | atlas（flowchart） | 无（零差异） |
| **S2b** | 组框 IR 物化 | Cross 投影写组框、`write_counter=0`、有组 flowchart 切 Atlas | atlas（**全部** flowchart） | 组框 Cross 微变 |
| **S3** | 通道进度量相 | Channel 成为 Occupant，删 SpaceBudget 回环 | atlas（含组图） | 边缝与间距明显变化 |
| **S4** | 组合相真实现 | 抽象通道图选路，删 repair/sanitize/repulse | atlas（全 Hierarchical） | **最大**：边几何全面重算 |
| **S5** | Dialect 收敛 | Hierarchical 吞 two_phase + group_divide | atlas | architecture 观感风险最高 |
| **S6** | 四内核归位 + 增量 | Tree/Sequence/Circular Dialect；Plan diff 增量 | atlas（全五图种） | mindmap/sequence 局部 |
| **S7** | 新门禁与收口 | 不变量门禁重建，恢复棘轮 | atlas | 收敛，无新退化 |

> S2 在推进中拆成 **S2a / S2b**（见 §4）。总览保留拆分，避免再把「管线骨架」误记成「杀死后验包围盒」。

**关键里程碑**：S4 结束 = 新管线成为 Hierarchical 系图种默认，**并作为评估门停下**（决策 D5，判据见 §11）；S6 结束 = 五图种全切；S7 结束 = §10 豁免退出。

---

## 2. Stage 0：地基与对拍

### 目标

在不改任何现有算法的前提下，把 Atlas 的**类型骨架**与**对拍工具**建起来。

### 交付物

| # | 交付 | 落点（建议） |
|---|------|--------------|
| 0.1 | `Provenance` 类型 + 几何量携带机制 | `layout/atlas/provenance.rs` |
| 0.2 | `Occupant` / `Demand` / `Substrate` 类型定义 | `layout/atlas/space.rs` |
| 0.3 | `AtlasPipeline` 空壳（转发 legacy） | `layout/atlas/pipeline.rs` |
| 0.4 | `PLOTGRAM_PIPELINE` 开关 + diagram attr 覆盖 | `layout/pipeline/plan.rs` |
| 0.5 | Shadow 对拍器 + `ShadowReport` | `layout/atlas/shadow.rs` |
| 0.6 | CLI 子命令 `plotgram shadow <图集>` → HTML 报告 | `plotgram-cli` |
| 0.7 | 门禁开关（**已完成**） | `benchmarks/scripts/gate-switch.sh` |

### `Occupant` 建议签名

```rust
pub enum OccupantKind { Node, GroupFrame, Channel, LabelBox }

pub struct Demand {
    pub min: f64,
    pub preferred: f64,
    pub grow: f64,      // 弹性权重；0 = 刚性
}

pub trait Occupant {
    fn id(&self) -> OccupantId;
    fn kind(&self) -> OccupantKind;
    fn demand(&self, axis: Axis) -> Demand;
    fn provenance(&self) -> &Provenance;
}
```

### 验收

- `PLOTGRAM_PIPELINE=shadow` 跑 product 集，报告显示**逐项零差异**（因为 atlas 还是转发 legacy）
- 报告 HTML 可读：节点 diff 表 + SVG 并排

### 回滚点

纯新增，回滚 = 删目录。

---

## 3. Stage 1：Plan IR 与表达力验证

### 目标

**先证明 `Plan` 装得下所有图种的离散决策，再谈换求解器。** 手段：让**旧管线**去填充 `Plan`。

### 交付物

| # | 交付 |
|---|------|
| **1.0** | **channel 合法化（前置，见下）**：27 号文 **L1–L4 + L8** —— 轨道分段、gate 绑定边界线（配对范围 `(o0,o1]`/`(r0,r1]`）、`span_weight` 真实化、gate 容量改输出、**搜索期作用域过滤**（编号 L*，勿与 24 号文 R1–R5 混淆） |
| **1.0b** | **合法性自反证检查器**（27 号文 **L6**）：`Substrate::verify_no_group_penetration()`，O(段数 × 组数) 静态检查 |
| 1.1 | `Plan` 结构：rank / order / gate / port / channel / merge 拓扑 |
| 1.2 | `Plan` 序列化 + 稳定哈希 + `diff()` |
| 1.3 | **Legacy Adapter**：从旧管线中间产物（`LayeredDraft`、`endpoint_map`、`LaneAssignment`、`MergeInterval`）反向构造 `Plan`。**注意**：Adapter 必须提供组矩形在 rank/order 网格上的覆盖区间（分段的输入），而不只是组树 |
| 1.4 | **Ink 原型**：`(Plan, 旧坐标) → 边几何`，与旧几何对拍 |
| 1.5 | 表达力缺口清单 |
| 1.6 | 探针三列口径改造（27 号文 **L5** / §5.1）+ 重采 25 号文全部数字 |

### 前置：channel 合法化（1.0 / 1.0b）

`layout/atlas/channel/` 原型已实现并出了[探针报告](25-Atlas-相I可行率探针报告-2026-07.md)。结论分两半：

- ✅ **粒度够、性能够**：三集 77 图 / 1152 边零「网格不可表达」；channel 本体 ≈35 ms。22 号文 §8 风险台账第一条结案。
- ❌ **合法性不成立**：H2「穿组不可表达」在 derive 产物上为假，实测 ≥2.3% 的边走穿组路径，且穿组是词典序**严格最优**（0 折点 vs 绕行 ≥2 折点）；gate 的 side 维度退化为装饰。

根因是省掉了**段（Segment）**维（22 号文 §3.1）。但**段维只解一半**：分段消灭「跨线跳跃」式借道，消灭不了「沿同一条线从左根段直穿到右根段」——后者 gate 同向 0 折点、几何最短，在 Q3/Q4 上双双最优，词典序必选（27 号文 §3.1）。因此必须同时上 **L8 搜索期作用域过滤**，否则 L1–L3 干完重跑探针 A2 会直接爆掉，且极易误判成「分段做错了」。

改造主要落在 `derive.rs` + `substrate.rs`；`search.rs` 只加一个作用域谓词（词典序 Dijkstra 本体、平局键、确定性不变）；`bundle.rs` / `Occupancy` **不动**。

**建议实施次序**：L1 分段 → L2 gate → **L8 过滤** → L6 检查器 → L3 span_weight → L4 容量 → L5 探针。L8/L6 插在 L3 之前，是因为 L3 会把边从最外框走廊赶回图内部（原型的穿组率之所以只有 2.3%，是代价退化把边都挤到了图外框），先立合法性约束再改代价，A2 的变化才归因得清。

**这不是新 Stage**，而是 Stage 1 的入场券：Legacy Adapter 若接到一个会穿组的基底上，后面所有对拍都在拿错误结果做基准。**1.0 / 1.0b 未过，不启动 1.3。**

### `Plan` 建议骨架

```rust
pub struct Plan {
    pub substrate: Substrate,                    // rank 轴 × order 轴骨架
    pub node_slots: IndexMap<NodeId, Slot>,      // (rank, order)
    pub group_scopes: IndexMap<GroupId, Scope>,  // 收缩层次 + rank 区间
    pub ports:    IndexMap<EdgeId, (PortId, PortId)>,
    pub gates:    IndexMap<EdgeId, Vec<GateId>>, // 跨组闸口序列
    pub channels: IndexMap<EdgeId, Vec<TrackSlot>>, // track 序列 = 路径拓扑
    pub bundles:  Vec<BundleId>,                 // 共享 track 后缀 = 合流
    pub provenance: ProvenanceMap,
}
```

### 关键验证问题（Stage 1 必须回答）

1. architecture 的 macro rank / equal-track 能否表达为 `group_scopes` + Profile？
2. sequence 的「布局阶段产边」能否表达为 `channels` 的退化形式（每条消息一条固定 track）？
3. mindmap 的径向布局 `Substrate` 怎么定义（rank = 层级深度，order = 角度序）？
4. `semantic_trunk_merge` 的 `MergeInterval` 能否无损映射为 `bundles`？
5. **分治网格 vs flat 网格**：25 号文的探针是统一 flat 口径，不镜像 `divide-conquer` / `two_phase` 的真实几何（三集中 27 张图受影响）。Adapter 接上真实 blueprint 后，这些图的合法可行率需**单独复测**——这是 flat 口径下唯一没被回答的问题。
6. **mindmap 深树**：stress-deep 在 flat 网格里峰值 lane 33（全部边压进少数几条缝）。27 号文 **L3** 修正 `span_weight` 后应大幅回落；若仍居高，说明径向图种确实需要另一套 `Substrate`（rank = 层级深度、order = 角度序），而不是通道图缺陷。

### 验收

**门 A（1.0 / 1.0b 合法化，硬）**——判据即 27 号文 §7：

| # | 判据 | 改造前 | 目标 | 由谁保证 |
|---|---|---|---|---|
| A1 | `verify_no_group_penetration()` 全集通过 | 无此检查 | 77 图全通过 | L1 / L6 |
| A2 | 借道穿「两端都不属于的组」的边数 | 27 / 1152 | **0** | **L8**（L1–L4 不足以达成） |
| A3 | 同一 gate 被同一条边穿越 ≥2 次 | 116 / 1152 | **0** | L2 |
| A4 | 合法单边可行率（三集） | 100%（含非法路径） | ≥ 99%，失败边逐条有解释 | 兜住 L8 的可行性副作用 |
| A5 | 峰值 lane demand（stress / product / demo） | 33 / 10 / 25 | 显著下降（27 号文 L3 效果度量，不设硬指标） | L3 |
| A6 | 端口争用成功率（27 号文 L5 生产口径） | 未测 | 出数并逐图分析；**不得**与 A4 合并成「争用也 100%」 | L5 |
| A7 | channel 本体耗时（三集合计） | ≈35 ms | ≤ 100 ms（段数上升的预算） | — |
| A9 | L1.1 切割边界用例单测 B1–B7 | 无 | 全部通过 | L1 |
| A10 | L8 副作用边界单测（组横跨全列时外框绕行仍可行） | 无 | 通过 | L8 |

A2 / A3 归零是**硬判据**——它们正是「由构造保证」这句话的定义。A3 不过查 L2；**A2 不过先查 L8 的作用域掩码，不要先怀疑 L1 分段**。

**门 B（Plan 表达力）**：

- 五图种 showcase 全部能构造出 `Plan`（Adapter 路径）
- **Ink 原型重放**：`Ink(Plan_from_legacy, 旧坐标)` 的边几何与旧几何差异 ≤ 容差（说明 `Plan` 无损）
- 缺口清单落文档；无法表达的项显式记债

### 回滚点

Adapter 与 Ink 原型均为旁路，主干管线不变。

> **这是整个方案风险最低、信息量最大的一步。** 如果 `Plan` 装不下某图种，在这里就暴露，代价是一个 Stage 而不是整个重构。

---

## 4. Stage 2：度量相统一（节点 + 组框）

### 目标

**杀死后验包围盒。** 组框四边成为相 II 的真变量。

> **进度（2026-07-27）**：拆成 **S2a（管线骨架）** + **S2b（组框 IR 物化）**，**二者均已完成**。遗留见下方「未收口 / 下阶段」。

### 交付物

| # | 交付 | 状态（2026-07-27 终） |
|---|------|----------------------|
| 2.1 | 打开 `GroupContainment` 投影 | ✅ Atlas 经 `LayoutSession::materialize` → `refine_group_frames(Cross)` → `project_group_constraints`；`model.rs` 注释已改 |
| 2.2 | 打开 `GroupSiblingSeparation` 投影 | ✅ **Cross 轴**已开；Main 轴 sibling 在冻结节点 + 水平并列组下不可行，**刻意不开**（见算法要点） |
| 2.3 | 逐轴交替求解框架 | ⚠️ **部分**：有组走「分治节点 + Cross IR 物化」；节点与组框尚未在同一 `solve()` 里联合优化（Stage 3 加深） |
| 2.4 | 删除 `compute_group_bounds` 在主路径的调用 | ✅ **Atlas flowchart**：只剩 `unrecorded` 种子 + IR 覆盖，**不计** `write_counter`。legacy recipe / architecture 仍用计权包围盒（非本 Stage 范围） |
| 2.5 | `Contract` 成为**唯一**坐标入口 | ⚠️ **部分**：Hierarchical/flowchart Atlas 已不经 `engines/`；architecture 自有装配留给 Stage 5 |
| 2.6 | `engines/` 目录清空 | ✅ 空壳已删 |

### 算法要点

- 有组 flowchart：**分治出节点**（与 legacy 节点零差异）+ [`LayoutSession::materialize`](../../crates/plotgram-core/src/layout/kernel/coordinate/session.rs)（Cross IR 投影写组框）
- 组包含 / 兄弟分离在 Cross 轴由 PAVA 投影；种子用 `compute_group_bounds_unrecorded`（无写权），最终几何以投影为准
- **Main 轴 sibling 暂缓**：水平并列组在冻结节点下对 Main sibling 约束不可行，会压破包容。Main 几何暂留种子；真正的 Main 联合求解随 Stage 3 通道变量一起做
- 裙边（`Skirt`）仍多处硬编码，Stage 3 Demand 化

### 主干切换（S2b 口径）

默认解析（`PipelineChoice::resolve`）：

- **全部 flowchart**（含有组）→ `Atlas`
- **architecture / er / state / 其他** → `Legacy`

回滚：`PLOTGRAM_PIPELINE=legacy` 或 DSL `pipeline: legacy`。

### 验收

| 项 | 口径 | 结果 |
|----|------|------|
| 无组 flowchart 节点 vs legacy | p95 ≤ 2px | ✅ p95=0 |
| 有组 flowchart 节点 vs legacy | 分治同源 | ✅ shadow 节点 diff = 0（loan-approval / customer-refund） |
| 有组 flowchart 组框 | IR 产出；与后验包围盒可有差 | ✅ 组 diff ≥1（Cross sibling 撑开）；记 §10 |
| `write_counter`（Atlas） | 0 | ✅ 日志 `write_counter=0`；阈值改为 0 |
| `engines/` | 删除 | ✅ |

### 已知退化窗口

- 组框相对 legacy 后验包围盒：Cross 方向可能被 sibling 投影撑开（预期）
- lint 可能随组框微变（loan-approval 有 lint 变化；customer-refund 持平）

### 回滚点

`PLOTGRAM_PIPELINE=legacy` 单开关回退。

### 未收口 / 下阶段（不挡进 Stage 3）

1. Main 轴组框联合求解（与通道 Occupant 同批更划算）
2. architecture / er / state 仍 legacy（Stage 5 Dialect）
3. `Contract` 唯一入口在 architecture 未收口
4. legacy `flowchart` recipe 仍可走计权 `compute_group_bounds`（仅当强制 `pipeline: legacy`）

---

## 5. Stage 3：通道进度量相

### 目标

**边缝与节点距同源。** `Channel` 成为 Occupant，`SpaceBudget` 回环消失。

> **进度（2026-07-27）**：**已收口，可进 Stage 4。** 真 Cross track 进 Main 轴 LP；分治有界抬缝；Atlas 旁路 SpaceBudget + border repulse；`RelaxationLadder` L1/L3 挂钩；lane 导出 API。模块物理删除与 Main 组框联合求解记债至 S4 / 后续。

### 交付物

| # | 交付 | 状态（2026-07-27 终） |
|---|------|----------------------|
| 3.1 | `track` 坐标变量进 `CoordinateProblem` | ✅ `VarKind::Track` + `solve_main_axis_with_cross_tracks` 生产路径 |
| 3.2 | 通道净空硬约束 | ✅ Cross track ↔ 两侧层顶 `MinSeparation`（`RouteDemand`） |
| 3.3 | lane 导出规则 | ✅ `lane_centers(track, n, pitch)`；落笔仍 legacy（S4 接 Ink） |
| 3.4 | Channel `Demand` 由 Plan lane 数决定 | ✅ `channel_band_width` / Occupant；选路 `commit` |
| 3.5 | 删 SpaceBudget / border_repulse | ⚠️ **Atlas 路径**：旁路 SpaceBudget 推节点 + `skip_border_repulse`；**模块暂留**供 legacy（S4 换选路后物理删除） |
| 3.6 | `RelaxationLadder` L0–L4 | ✅ L1 缩 pitch → L3 启发式层顶；L2/L4 策略体待 S4 相 I 回退 |

### 算法要点

- **flat**：`LayeredKernel` → `build_channel_metric` → `solve_main_axis_with_cross_tracks` → BK Cross + Main 层顶物化；Main 走廊 Demand → `expand_layer_order_gaps`
- **分治**：全局 slots 选路；**仅 Vertical 拼组**做 Cross 推 Y（Horizontal 跳过，防 rank 邻接误推）；单缝 cap ≈88px
- Plan channels 仍由通道图 `route_candidates`（**未换选路**——留给 S4）

### 验收

| 项 | 口径 | 结果 |
|----|------|------|
| 真 track LP | flat 日志 `atlas-main-axis-tracks` | ✅ |
| 简单图零差 | Demand ≤ 基线 | ✅ `linear-chain` / `fan-out-four` |
| 抬缝可解释 | 画布 Δy | ✅ user-auth +12 / password-reset +44 / ticket-triage +26 |
| 有组 Horizontal | 不炸画布 | ✅ loan 节点 p95=26 · refund p95=44（Main 走廊 X） |
| Atlas SpaceBudget / repulse | 主路径不调 | ✅ |
| `write_counter` | 0 | ✅ |

### 已知退化窗口

- 画布变大、间距变松（正确性换来的）
- 边几何仍 legacy，与新缝可能不完全协调 → **Stage 4 解决**
- Main 轴组框联合求解仍未做（组框仍 Cross IR + 种子）
- `space_budget_guard` / `border_repulse` 源码仍在（仅 Atlas 旁路）

### 回滚点

`PLOTGRAM_PIPELINE=legacy`。

### 进入 Stage 4 的前置（本 Stage 出口）

1. ✅ Channel 在度量相有 Demand / Occupant / Track 变量
2. ✅ Atlas 不再依赖 SpaceBudget 回环撑缝
3. ✅ 影子对拍可解释；无「静默不可行」
4. ⬜ 选路仍为 legacy 正交链 —— **正是 S4 要换的**

---

## 6. Stage 4：组合相真实现

> **本方案的核心 Stage，也是退化窗口最大的一步。**
>
> **进度（2026-07-27）**：**主链替换已落地（4a–4c）**。flowchart Atlas 落笔走 `channel_metric` Plan → `materialize_edges`（Ink）；切断 `OrthogonalRecipe` / Coordinator repair。MCF Gate 分配与三块全局协调记债；`edge_routing_orthogonal/` 物理瘦身留给后续 PR。

### 目标

用抽象通道图选路替换正交路由主链；删除全部补救阶段。

### 交付物

| # | 交付 | 状态（2026-07-27） |
|---|------|-------------------|
| 4.1 | 抽象通道图构建接生产 blueprint | ✅ `build_channel_metric` / derive（L1–L8） |
| 4.2 | Gate 分配（MCF） | ⚠️ Occupancy 累计 + 相 II 撑开（D6）；完整 MCF 记债 |
| 4.3 | 通道选路 | ✅ `route_candidates`（词典序 Dijkstra）生产路径 |
| 4.4 | 全局协调 ≤3 轮 | ❌ 记债 |
| 4.5 | 合流 = bundle 后缀 | ✅ `detect_bundles` + Ink `apply_bundle_suffixes` |
| 4.6 | 删 repair/sanitize/path_solver | ✅ **Atlas 路径**不再调用；模块暂留供 legacy |
| 4.7 | Ink 相正式化 | ✅ `materialize_edges` → `EdgeLayout` |
| 4.8 | 缩减 `edge_routing_orthogonal/` | ⚠️ Atlas 生产不再进入；目录物理删减后续 |

### 算法要点（落地口径）

- **一次选路**：度量相 `ChannelMetric` 透出至 `AtlasSolveOutput.channel`；落笔复用，不重跑
- **端口 / lane**：`RouteOutcome.{from,to}_port` → `Plan.ports`；`assign_lane_indices` + `lane_centers`
- **坐标**：flat 用 Main LP `track_coords`；divide 用 InkContext 间隙表（L3 留痕）
- **自环 / 无通道**：几何 stub，禁止静默掉边
- **标签**：`LabelSolver` 仍在 Ink 折线上跑

### 验收

| 项 | 结果 |
|----|------|
| flowchart 出 SVG | ✅ `linear-chain` / `user-auth` / `loan-approval` |
| Atlas 无 OrthogonalRecipe | ✅ `pipeline.rs` Ink 路径 |
| `cargo test -p plotgram-core --lib` | ✅ 948 passed |
| 边几何 vs legacy | **全面变化**（预期）；§10 登记 |
| 穿组 / det | 待持续 shadow；优先样本见下 |

**优先回归样本**：`demo.plotgram-core-mod-deps`、`product.cloud-native`、`stress.layout-stress-nested` 等（architecture 仍 legacy，通道压力图待 Stage 5）。

### 已知退化窗口

- 边走向与 legacy 明显不同（拓扑选路 vs 像素选路）
- Ink 首版 lane / 间隙近似；美学指标可短期变差
- divide 路径无 Main track LP 坐标

### 回滚点

`PLOTGRAM_PIPELINE=legacy` 或 DSL `pipeline: legacy`。

### 未收口（不挡宣告主链替换）

1. MCF Gate 分配（4.2）
2. Ordering/Gate/Channel 三块坐标下降（4.4）
3. `edge_routing_orthogonal/` 物理删减（4.8）
4. architecture 切 Atlas（Stage 5）→ **S5 已完成**

---

## 7. Stage 5：Dialect 收敛

> **进度（2026-07-27）**：**已收口。** `Dialect` + `HierarchicalDialect` + Scheme 注册表；Weak/StrongMacro Contraction 吞掉 `group_divide` / `two_phase`；flowchart + architecture 默认 Atlas + Ink；`group_frame`→Profile 适配；DSL 嵌套放宽至 8 层。ER/state Atlas 见 Stage 6。

### 目标

兑现 doc 21 §5.0：flowchart 与 architecture 一套布局、一套路由。

### 交付物

| # | 交付 | 状态 |
|---|------|------|
| 5.1 | `Dialect` trait + `HierarchicalDialect` | ✅ `atlas/dialect/` |
| 5.2 | `Contraction` StrongMacro（原 two_phase） | ✅ `dialect/contraction/strong_macro/` |
| 5.3 | Weak（原 group_divide）并入同一门面 | ✅ `dialect/contraction/weak.rs` |
| 5.4 | Profile：`group_policy` / `group_sizing` / `group_align` / `density` | ✅ |
| 5.5 | **删除** `recipes/architecture/two_phase/`、`recipes/flowchart/group_divide.rs` | ✅ |
| 5.6 | `Scheme`：`hierarchical-flow-ortho` / `hierarchical-arch-equal-track-ortho` | ✅ |
| 5.7 | `group_frame` → Profile 适配器 | ✅ `dialect/frame_adapt.rs` |

### 观感规则 → Profile 落点

| 原 two_phase / group_divide 行为 | Profile / 落点 |
|----------------------------------|---------------|
| Macro rank 超级图分层 | `group_policy: StrongMacro` |
| 组间垂直堆叠 | `group_policy: Weak` |
| Equal-track / 等宽条带 | `group_sizing: Equal`（arch Scheme 默认；thread-local override 进 StrongMacro） |
| Fit 贴合 | `group_sizing: Fit`（flow 默认） |
| Hub 居中 / client 共线 | `hub_client_align`（arch 默认开） |
| 行对齐 Start/Center | `group_align` |
| 疏密 / pair gap | `density` (+ gap_scale) |
| 跨组 nudge / pendant / EGB gutters | StrongMacro 内保留；legacy 正交测试强制 `pipeline: legacy` |
| Sugiyama preset | `HierarchicalPreset::{Flowchart,Architecture}` |

### 验收

- `recipes/architecture` 与 `recipes/flowchart` **不再有独立的分层实现**（有组路径均走 Dialect Contraction）
- `PLOTGRAM_GATES=on ./benchmarks/scripts/check-semantic-isolation.sh` → OK
- architecture 默认 Atlas；回滚：`pipeline: legacy`
- 观感退化记 §10（边 Ink 化与节点坐标相对 legacy 可解释偏移）

---

## 8. Stage 6：四内核归位 + 增量

> **进度（2026-07-27）**：**已收口。** Tree / Sequence / Circular Dialect + state 双 Scheme；全图种默认 Atlas；Plan diff 增量取代 FrozenRoutingSolution 指纹增量；`circular` 布局注册进 registry。

### 交付物

| # | 交付 | 状态 |
|---|------|------|
| 6.1 | `TreeDialect`（mindmap = Tree + radial Profile） | ✅ `dialect/tree.rs` · Scheme `tree-radial-organic` |
| 6.2 | `SequenceDialect`（BuiltinEdges） | ✅ `dialect/sequence.rs` · `sequence-builtin-edges` |
| 6.3 | `CircularDialect` + registry 注册 `circular` | ✅ |
| 6.4 | state 双 Scheme：`hierarchical-state` / `circular-state` | ✅ Dialect 承接 FAS/`layout: circular` |
| 6.5 | Plan diff 驱动增量 | ✅ `compute_layout_incremental(&Plan)` |
| 6.6 | Integrated/Standalone = 是否复用上次 Plan | ✅ |
| 6.7 | 删 FrozenRoutingSolution 指纹增量生产路径 | ✅ Coordinator 不再 `plan_incremental_reuse` |

### 验收

- 五图种 + ER 默认 Atlas；`pipeline: legacy` 仍可回滚（开关保留至 Stage 7）
- 增量：同图重跑 `PlanDiff` 空（`incremental_zero_diff_render_is_byte_identical`）
- `PLOTGRAM_PIPELINE=legacy` 未物理删除（记债 Stage 7）

---

## 9. Stage 7：新门禁与收口

### 目标

门禁从「grep 脚本 + 数值棘轮」升级为**架构不变量检查**，然后关掉 §10 豁免。

### 新门禁设计

| 不变量 | 检查方式 | 取代 |
|--------|----------|------|
| 相 II/III 不做离散决策 | 类型级：`Metric` / `Ink` 阶段无分支 API；`Plan` 为 `&` 只读 | `check-group-writes.sh`（部分） |
| 每个几何量有 Provenance | 运行时断言：channel Provenance 覆盖率 = 100% | 手册「先追写权」法则 |
| 内核无 `DiagramType` | 保留 `check-semantic-isolation.sh` | 沿用 |
| 词典序无跨层求和 | `LexCost` 类型强制；旧标量罚项表 WARN | `check-penalty-ratio.sh` |
| 拓扑合法性由构造保证 | L6 + `verify_route_scope` 表驱动 | repair loop |
| 确定性 | 沿用（`BTreeMap` / 显式排序 + 双跑 hash） | 沿用 |

落地脚本：`check-provenance-coverage.sh` · `check-atlas-phase-api.sh`（均 `source gate-switch.sh`）。

### 收口动作

1. ~~`gate-switch.sh` 的 `GATES_DEFAULT` 改回 `on`~~ ✅
2. ~~删 `.github/workflows/ci.yml` 的 `PLOTGRAM_GATES` 与 `continue-on-error`~~ ✅
3. ~~重打基线：`./benchmarks/snapshot.sh --tag atlas-final`~~ ✅
4. ~~`AGENTS.md` §10 标记为已失效~~ ✅
5. ~~手册按新三相改写 §3「正交管线现状地图」~~ ✅
6. ~~更新 [`layout-routing-architecture.html`](../architecture/layout-routing-architecture.html)~~ ✅
7. ~~删除 `PipelineChoice::Legacy` / `pipeline: legacy`~~ ✅（保留 Atlas + Shadow）

---

## 10. 已知退化清单（滚动维护）

每个 Stage 结束时在此追加。格式：`[Stage] 图名 · 指标 · legacy→atlas · 结论（接受/待修/已修）`

| Stage | 图 | 指标 | 变化 | 结论 |
|-------|----|------|------|------|
| S1 前置 | 三集 1144 边（合法化后；8 自环按 L7-T5 排除） | 借道穿无关组 | 27 → **0** | **已修**：L1/L8；门 A / A2 过（28 号文） |
| S1 前置 | 三集 | 同 gate 被同边穿 ≥2 次 | 116 → **0** | **已修**：L2；门 A / A3 过 |
| S1 前置 | `demo.plotgram-core-mod-deps` | 穿组边 / 成功边 | 16/29 → **0** | **已修**：首要回归样本已绿 |
| S1 前置 | `stress.layout-stress-deep` | 峰值 lane demand | 33 → **25** | **已修（效果度量）**：L3 `span_weight` 生效；仍偏高，径向 Substrate 另议（Stage 1 验证问题 6） |
| S1 前置 | 三集 | gate 容量扫描表 | 有效容量伪影消除 | **已修读数**：L2/L4 后按三列口径重采（25 号文 §8） |
| S1.3 | product 集 7 图 | 同层边（same_layer_edge） | Plan 无对应字段，记 UnmappedDecision | **接受**：同层边走节点旁小环，不进通道图；Stage 3 补充表达 |
| S1.3 | Sequence×4 + Mindmap×1 | Plan 构造 | Adapter 返回 UnsupportedDiagramType | **接受**：设计决定，此二图种不走 LayeredKernel |
| S1.4 | product 集 | Ink 拓扑一致率 | 简单图 100%，复杂图 0–71%（均值≈30%） | **接受**：Ink 取间隙中心近似，非精确 lane；**S2b 组框真变量落地后重采**（勿写「S2 坐标求解后」——S2a 未改坐标语义） |
| S2a | engines/ 目录 | 代码组织 | 迁入 kernel/coordinate/；空壳已删 | **完成** |
| S2a | 无组 flowchart×7 | 节点坐标 diff | Atlas vs Legacy p95=0.00px, max=0.00px | **完成** |
| S2a | 无组 flowchart | 默认管线 | Legacy → Atlas | **完成** |
| S2a | architecture/er/state | Atlas 接管 | 仍走 Legacy | **接受**：待 Stage 5 |
| S2b | 有组 flowchart | 组框来源 | 计权包围盒 → Cross IR 物化（`write_counter=0`） | **完成** |
| S2b | `demo.loan-approval-workflow` | 节点 / 组 / lint | 节点 0 · 组 diff 2 · lint 有变化 · 画布 Δ0 | **接受**：Cross sibling 撑开组框（预期） |
| S2b | `demo.customer-refund-process` | 节点 / 组 / lint | 节点 0 · 组 diff 2 · lint 持平 · 画布 Δ0 | **接受** |
| S2b | Main 轴组框联合求解 | — | 暂留种子几何 | **记债**：与 Stage 3 真 track 变量同批 |
| S3 首切片 | flat flowchart 通道抬缝 | 层缝 / 画布 | Cross lane Demand → `inflate_layer_gaps`；Atlas 旁路 SpaceBudget | **完成（已由真 track LP 取代主路径）** |
| S3 首切片 | `product.linear-chain` / `smoke.fan-out-four` | 节点 | 零差异（Demand ≤ 启发式基线） | **接受** |
| S3 首切片 | `product.user-auth` | 节点 / 画布 | 节点 diff 5 · p95=16px · 画布 Δy=12 | **接受**：缝抬高预期 |
| S3 首切片 | `product.password-reset` | 节点 / 画布 | 节点 diff 9 · p95=44px · 画布 Δy=44 | **接受** |
| S3 首切片 | `product.ticket-triage` | 节点 / 画布 | 节点 diff 7 · p95=26px · 画布 Δy=26 · lint 持平 | **接受** |
| S3 | 真 Track Main LP | flat | `solve_main_axis_with_cross_tracks` + BK 层顶 | **完成** |
| S3 | Atlas `skip_border_repulse` | 路由 | Coordinator 只 snap | **完成** |
| S3 | `RelaxationLadder` | 度量相 | L1 pitch / L3 启发式回退 | **完成** |
| S3 | 分治 Horizontal | loan / refund | 跳过 Cross Y 推缝；Main 走廊 X 有界差 | **接受**（loan p95=26 · refund p95=44） |
| S3 | 模块物理删除 / Main 组框联合 | — | 记债 | **S4 / 后续** |
| S4 | flowchart 落笔 | 路由主链 | OrthogonalRecipe → Ink `materialize_edges` | **完成** |
| S4 | `product.linear-chain` | 节点 / 画布 | 节点 0 · 画布 Δy（边几何变） | **接受** |
| S4 | `product.user-auth` | 节点 / lint | 节点 p95≈17 · lint 变化 · 画布变大 | **接受**：边全面重算 |
| S4 | `demo.loan-approval-workflow` | 节点 / 组 | 节点 p95=26 · 组 diff 3 | **接受**（分治 + Ink） |
| S4 | MCF / 三块协调 / ortho 目录删 | — | 记债 | **后续** |
| S5 | Dialect / Scheme / Contract | 类型 | `atlas/dialect/` 落地 | **完成** |
| S5 | Contraction | 代码 | 删 `two_phase/` + `group_divide.rs`；迁入 dialect | **完成** |
| S5 | architecture 默认管线 | PipelineChoice | Legacy → Atlas + Ink | **完成** |
| S5 | `demo.plotgram-core-mod-deps` | shadow | 节点 diff 20 · p95≈102 · 组 8 · 边 Ink | **接受**：StrongMacro+Ink 预期 |
| S5 | `product.cloud-native` | render | scheme=hierarchical-arch-equal-track-ortho 出 SVG | **完成** |
| S5 | 正交专项测试 | — | 强制 `pipeline: legacy` | **接受** |
| S5 | DSL group 嵌套 | 上限 | 2 → 8 层（D4） | **完成** |
| S5 | ER / state Atlas | — | 仍 legacy | **已由 S6 收口** |
| S6 | 四 Dialect | 默认管线 | 全图种 → Atlas | **完成** |
| S6 | `circular` registry | 布局算法 | 一等注册 | **完成** |
| S6 | state Scheme | 决策 | Dialect `state_prefers_circular` | **完成** |
| S6 | Plan diff 增量 | API | `compute_layout_incremental(&Plan)` | **完成** |
| S6 | FrozenRoutingSolution 增量 | 生产 | Coordinator 禁用 | **完成** |
| S6 | smoke mindmap/sequence/state/er | render | dialect 日志正确出 SVG | **完成** |
| S7 | 新不变量门禁 | scripts | provenance / phase-api + 旧 check 调口径 | **完成** |
| S7 | `GATES_DEFAULT` / CI | 开关 | on；删豁免 | **完成** |
| S7 | `PipelineChoice::Legacy` | 生产 | 删除；保留 Atlas + Shadow | **完成** |
| S7 | 基线 / 文档 | tag | `atlas-final`；手册 §3 + HTML；§10 失效 | **完成** |
| S7 | compare vs 旧基线 | 质量/穿组几何 | Ink 相对 frozen-routing 有界退化；note 显式 raise product / stress | **接受**（基线 note） |
| S7 | MCF / ortho 物理删 / Main 组框 | — | 记债 | **后续** |

---

## 11. 决策记录

| # | 决策 | 结论 | 状态 |
|---|------|------|------|
| **D1** | 代码组织 | **`layout/atlas/` 新子树 + `PLOTGRAM_PIPELINE` 开关**；新旧两条管线共存于同一进程，供影子对拍。每个 Stage 结束关掉对应旧路径，双轨不跨 Stage | ✅ 已定 |
| **D2** | 相 II 求解器 | **扩展现有 PAVA + Dykstra + 投影梯度**，不引入 LP/QP crate。逐轴分解后每轴为一维凸问题；保 WASM 体积与确定性 | ✅ S2a/S2b：无组同源零差异；有组 Cross 组框投影已实证。Main 联合求解记债 |
| **D3** | Stage 4 退化容忍 | **正确性硬、美学软**：product 集穿组 = 0 与 `det = true` 不可退（Post-S7 已闭合 Ink↔lint；CI `check-product-penetration.sh`）；crossings / bends 允许有界退化 | ✅ 已定；Post-S7 对齐 |
| **D4** | group 嵌套深度 | DSL 上限由 2 放宽至 **8 层**（`stmt.rs`）；更深仍报结构违规。Contraction 本身不限深 | ✅ S5 |
| **D5** | 节奏 | **先推到 Stage 4 停下评估**。彼时新管线已成型、旧补救阶段已删，是天然决策点；S5–S7 视评估结果再启动 | ✅ 已定 |
| **D6** | gate 容量的性质 | **容量是输出，不是输入**。相 I 只累计 `crossing_demand`，相 II 用「边界段长 ≥ demand × pitch」撑开，撑不开走 `RelaxationLadder`。固定容量仅作诊断扫描通道。理由：「容量 = 边长 / 最小间距」是相 I 拿不到的几何量；原型用「容量 = 该组跨界边数」绕过，等于把约束设成恒真（27 号文 F3） | ✅ 已定（22 号文 §5.1 I.4 已改） |
| **D7** | 通道图的维度取舍 | **段（Segment）维必需，lane 维可延后**。lane 分配本质是度量相的事，相 I 不必区分同段第几条车道；但段维是 H2、gate 语义、Q4 长度三者共同的前提，省掉即三者同时失效（27 号文 §3） | ✅ 已定（22 号文 §3.1 已改） |
| **D8** | 「由构造保证」的证明义务 | **凡在文档或代码里写「由构造保证」，必须配一个 O(n) 静态检查器**，并纳入对应 Stage 的验收门。原型的 H2 声称由构造保证却无检查器，违规静默存在了整轮探针 | ✅ 已定（Stage 1 交付 1.0b、Stage 4 验收三轨） |
| **D9** | 「穿组不可表达」由什么构成 | **静态 + 动态两条，缺一不可**：① 基底侧——段被组边界切开，无根段几何贯穿组体（L1，检查器 L6 证）；② 搜索侧——边只能使用与其两端作用域链相容的段（L8）。只做 ①，「沿同一条线直穿组体」仍是 0 折点 + 几何最短的双优路径，A2 无机制支撑。**取硬过滤而非软罚项**：软罚项等于把 A2 退回软指标，与「不可表达」的立意冲突。代价是少数边真正 `Infeasible`，由 A4「≥99% + 逐条解释」接住，**禁止兜底路径掩盖** | ✅ 已定（27 号文 §3.1 / L8） |

### 由 D5 派生的阶段门

**Stage 4 评估门**（S5 启动前必须过）：

1. product 集正确性零违规（穿组 = 0、`det = true`）
2. 相 I 成功率 100%；相 II 松弛级别 L0 占比 > 90%
3. 美学退化项逐图有解释，写入 §10 清单
4. 性能不劣于 legacy 的 1.3×
5. `routing/` 代码量实际下降（今天 ~30k 行 / 82 文件）

任一项不过 → 停在 S4，不进 Dialect 收敛。

### 当前进度

- [x] 门禁关闭（[`gate-switch.sh`](../../benchmarks/scripts/gate-switch.sh) + `AGENTS.md` §10）
- [x] 设计定稿（22 总纲 / 23 本文）
- [x] channel 原型 + 可行率/性能探针（[25 号文](25-Atlas-相I可行率探针报告-2026-07.md)）—— 提前验证 22 号文风险台账第一条
- [x] channel 审查（[27 号文](27-Atlas-channel模块审查与改造需求-2026-07.md)）
- [x] **channel 合法化 L1–L8**（[28 号文](28-Atlas-channel合法化实现总结与审查-2026-07.md)）：门 A / A1–A10 全过；路径级 verifier（[29 号文](29-Atlas-路径级作用域自反证verifier报告-2026-07.md)）
- [x] Stage 0 地基：交付 0.1–0.6 全部落地（provenance / space / AtlasPipeline 空壳 / `PipelineChoice` 三态开关含 DSL `pipeline:` attr / shadow 对拍器 / `plotgram shadow`），product 集零差异
- [x] Stage 1 Plan IR + Legacy Adapter + Ink 原型：1.0–1.4 / 1.6 落地（合法化 + Plan + Adapter + Ink + 探针重采）；门 A / 门 B 过。1.5 表达力缺口见 §10 S1.3
- [x] **Stage 2a 度量相骨架**：engines→kernel、Atlas 接管无组 flowchart、默认解析切换、shadow 零差异
- [x] **Stage 2b 组框 IR 物化**：分治节点 + `LayoutSession` Cross 投影；全部 flowchart 默认 Atlas；`write_counter=0`；空壳 `engines/` 已删
- [x] **Stage 3 通道进度量相**：**已收口**——真 track Main LP、分治有界抬缝、Atlas 旁路 SpaceBudget/repulse、Ladder L1/L3、lane 导出；模块物理删除与 Main 组框联合记债
- [x] **Stage 4 组合相真实现（主链替换）**：Plan→Ink→EdgeLayout；切断正交主链；bundle 后缀；LabelSolver 保留；MCF/全局协调/目录瘦身记债
- [x] **Stage 5 Dialect 收敛**：HierarchicalDialect + Contraction；architecture 默认 Atlas；删 dual-track 分层目录；Scheme + group_frame 适配
- [x] **Stage 6 四内核归位 + 增量**：Tree/Sequence/Circular；全图种 Atlas；Plan diff；废 FrozenRouting 指纹增量
- [x] **Stage 7 新门禁与收口**：不变量检查器；开闸；删 Legacy；`atlas-final`；设计期结束

---

## 12. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-26 | 初稿：Stage 0–7、影子对拍机制、验收口径、退化窗口、待决策项 |
| 2026-07-26 | 按 channel 实证（[25](25-Atlas-相I可行率探针报告-2026-07.md) / [27](27-Atlas-channel模块审查与改造需求-2026-07.md)）修订：Stage 1 新增前置交付 1.0 / 1.0b（合法化 + 自反证）与门 A 硬判据、1.6 探针双口径；关键验证问题补分治网格复测与 mindmap 深树两条；Stage 4 验收改为穿组双轨证明 + 容量口径条款 + 优先回归样本，性能要点补实测；§10 登记五项已识别缺口；§11 新增 D6（容量是输出）/ D7（段维必需）/ D8（构造保证的证明义务） |
| 2026-07-26 | 随 27 号文商榷修订：合法化需求改称 **L1–L4/L6**（与 24 号文 R1–R5 解耦）；1.6 / 门 A 补端口争用列与三列探针口径 |
| 2026-07-26 | 随 27 号文第二轮修订（借道缺口）：1.0 交付扩为 **L1–L4 + L8**，前置说明补「段维只解一半」的推演与实施次序 L1→L2→L8→L6→L3→L4→L5；门 A 增「由谁保证」列与 A9/A10，并注明 A2 不过先查 L8；Stage 4 正确性口径由双轨改**三轨**；§11 新增 **D9**（穿组的构造保证 = 静态归属 + 动态过滤，取硬过滤） |
| 2026-07-27 | Stage 2 推进记录（初记为「完成」）：engines→kernel、atlas/solve.rs、无组 flowchart 默认 Atlas、product 7 张零差异；§10 追加 S2 条目 |
| 2026-07-27 | **进度校正**：将「Stage 2 完成」拆为 **S2a 已完成 / S2b 未完成**…… |
| 2026-07-27 | **S2b 落地**：`LayoutSession::materialize`（unrecorded 种子 + Cross IR）；`divide_flowchart_nodes` 节点-only；Atlas 接管全部 flowchart；默认解析扩到有组；`write_counter` 阈值 0；删空壳 `engines/`；shadow 基线写入 §10；Main 轴联合求解记债至 Stage 3 |
| 2026-07-27 | **S3 首切片**：`channel_metric` + Cross 抬缝进 `solve_atlas_flat`；Atlas 旁路 SpaceBudget；Adapter `occupancy.commit`；`VarKind::Track` / `channel_ir` / `RelaxationLadder` 骨架；§5/§10 诚实登记未收口 |
| 2026-07-27 | **S3 收口**：`solve_main_axis_with_cross_tracks` 真 track LP；分治 Vertical 推缝 + Horizontal 跳过 + 单缝 cap；Main 走廊 order 撑开；`skip_border_repulse`；Ladder L1/L3；`lane_centers`；宣告可进 Stage 4 |
| 2026-07-27 | **S4 主链替换**：`RouteOutcome` 端口 + `Plan.lane_indices`；`materialize_edges`；Atlas pipeline 切断 Coordinator 正交链；bundle 后缀；§6/§10 登记；MCF/4.4/目录瘦身记债 |
| 2026-07-27 | **S5 Dialect 收敛**：`atlas/dialect/`（Profile/Scheme/Contract/Contraction）；删 `two_phase/`+`group_divide.rs`；architecture 默认 Atlas+Ink；观感映射表；`group_frame` 适配；嵌套 8 层；当前位置 Stage 6 |
| 2026-07-27 | **S6 四内核 + 增量**：Tree/Sequence/Circular Dialect；state 双 Scheme；全图种默认 Atlas；Plan diff 增量；废 FrozenRouting 指纹；circular 注册；当前位置 Stage 7 |
| 2026-07-27 | **S7 新门禁与收口**：provenance/phase-api 门禁；`GATES_DEFAULT=on`；删 CI 豁免与 `PipelineChoice::Legacy`；正交专项改 Atlas 不变量；手册 §3 + architecture HTML；基线 `atlas-final`；AGENTS §10 失效；设计期结束 |
