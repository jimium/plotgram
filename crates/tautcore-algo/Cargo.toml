[package]
name = "tautcore-algo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared graph-drawing algorithm parts (VPSC, FAS, metrics, …) — no layout orchestration"

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
